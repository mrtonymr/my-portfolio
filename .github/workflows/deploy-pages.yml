name: Deploy GitHub Pages

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Install portfolio dependencies
        run: npm ci

      - name: Install Developer Toolbox dependencies
        run: npm ci
        working-directory: developer-toolbox

      - name: Build portfolio
        run: npm run build:gh-pages

      - name: Build Developer Toolbox
        run: npm run build
        working-directory: developer-toolbox

      - name: Bundle Developer Toolbox into Pages artifact
        run: |
          mkdir -p dist/cursor-test/browser/developer-toolbox
          cp -R developer-toolbox/dist/developer-toolbox/browser/. dist/cursor-test/browser/developer-toolbox/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: dist/cursor-test/browser

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
